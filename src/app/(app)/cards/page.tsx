// Phase 2 placeholder so the sign-in round trip has somewhere to land.
// Replaced by the wallet grid in phase 5.
export default function CardsPage() {
  return (
    <h1 className="text-2xl font-semibold tracking-tight text-foreground">
      Wallet
    </h1>
  );
}
